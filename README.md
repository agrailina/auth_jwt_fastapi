# FastAPI Auth Project - Исправленная версия

Этот проект представляет собой сервис аутентификации пользователя на фрэймворке FastAPI с использованием JWT токенов.

## 📋 Стек технологий
- Веб-фреймворк: FastAPI
- База данных: SQLite (легко заменяемая на другую SQL-СУБД)
- Авторизация/Аутентификация: bcrypt для хеширования паролей, python-jose для защиты данных с использованием JWT

## 🏗️ Структура проекта

```bash
auth_jwt_fastapi/
├── app/
│   ├── __init__.py
│   ├── main.py              # Основной файл для запуска приложения
│   ├── auth.py              # JWT аутентификация
│   ├── models.py            # Модели данных для авторизации
│   ├── schemas.py           # Pydantic схемы
│   ├── dependencies.py      # Зависимости 
│   ├── database.py          # Подключение к базе данных и управление сессиями
│   ├── routes/              # Папка с роутерами
│   │   ├── __init__.py      # Инициализация пакета routes
│   │   ├── auth.py          # Роутер аутентификации (API)
│   │   ├── users.py         # Роутер пользователей (API)
│   │   └── pages.py         # Роутер HTML страниц
│   └── templates/           # HTML шаблоны
│       ├── index.html       # Главная страница
│       ├── login.html       # Страница входа (логин)
│       ├── register.html    # Страница регистрации
│       └── dashboard.html   # Личный кабинет (после авторизации)
├── Dockerfile               # Docker образ приложения
├── docker-compose.yml       # Docker Compose для запуска с БД
├── requirements.txt         # Зависимости проекта
├── .env                     # Переменные окружения
└── README.md                # Документация проекта
```

## 📋 API Endpoints
- GET / - Главная страница

- GET /register - Страница регистрации

- GET /login - Страница входа

- GET /dashboard - Личный кабинет

- POST /api/register - API регистрации

- POST /api/login - API входа

- POST /api/logout - API выхода

- GET /api/me - Информация о текущем пользователе

- GET /api/users - Список пользователей

## 🚀 Быстрый старт

### Вариант 1: Простая установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/agrailina/auth_jwt_fastapi.git
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Запустите приложение:
```bash
uvicorn app.main:app --reload
```


### 🐳 Вариант 2: Установка через Docker Compose 

1. Клонируйте репозиторий:
```bash
git clone https://github.com/agrailina/auth_jwt_fastapi.git
```

2. Запуск:
```bash
# Запуск в фоне
docker-compose up -d

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f
```

3. Проверка работоспособности:
```bash
http://localhost:8000/
```
